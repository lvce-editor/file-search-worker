import * as QuickPickEntryId from '../QuickPickEntryId/QuickPickEntryId.ts'

export const state = {
  provider: QuickPickEntryId.File as number,
  prefix: 'string-that-should-never-match-another-string',
}
