import { RendererWorker } from '@lvce-editor/rpc-registry'

export const { invoke, set, invokeAndTransfer, activateByEvent, showContextMenu, closeWidget, openWidget, setFocus, openUri, getFileIcon, getFolderIcon, showErrorDialog } = RendererWorker
